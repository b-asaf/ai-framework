# Project Overview — Patterns
> Load when: implementation agents (backend, frontend, ui, db, api),
> code-reviewer, plan-reviewer, architect.
> Single source of truth for pattern enforcement. Updated incrementally by agents.

## Existing patterns & conventions
[Discovered from codebase — naming, file structure, import style, etc.]

## Discovered pattern registry

> Updated incrementally by agents whenever a new domain is encountered.
> See the `pattern-enforcement` skill for how entries are added and what each status means.

| Domain | Pattern | Example | Status |
|---|---|---|---|
| [populated by agents during discovery] | | | |
