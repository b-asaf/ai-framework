---
description: Compact the current session — save a handoff document and append to session-summary.md. Use when the session is getting long or before switching context.
---

Run the handoff procedure:

1. Load the `handoff` skill.
2. Produce a handoff document saved as `.handoff-[task-slug]-[date].md`.
3. Append a short dated entry (5-10 lines: what changed, decisions, pending) to `docs/session-summary.md`.
4. Tell the developer the handoff file name and how to resume in a new session.
